import { NgbModalOptions } from "@ng-bootstrap/ng-bootstrap";

export const ngbModalOptions: NgbModalOptions = {
  centered: true,
  windowClass: 'modal modal-primary custom-modal',
  backdrop: 'static',
  animation: true
}
